{
  "name": "mtg-keyword-codex",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "backend": "bun backend/index.ts",
    "backend:dev": "bun --watch backend/index.ts",
    "backend:scryfall-demo": "bun backend/scryfallDemo.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "bun backend/db/migrate.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.8"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.1",
    "fastify": "^5.7.1"
  }
}
